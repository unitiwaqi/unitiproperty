# Blender autosave backups — not meant to be versioned
*.blend1
*.blend

# OS
.DS_Store

# Claude Code local runtime state
.claude/

# Node (once the app is scaffolded)
node_modules/
dist/
.env
.env.local

output/
